{
  "name": "n8n-nodes-myob",
  "version": "0.1.0",
  "description": "n8n custom node for MYOB AccountRight",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "n8n-workflow": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "rimraf": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}